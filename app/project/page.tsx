"use client";

export default function ProjectPage() {
  // need to declare a list of the collected projects

  // initialize list with the projects gotten from API call on page open - useEffect

  // keep track of loading and error states just in case

  return (
    <div>
      <h1>Projects</h1>
      list of projects here
    </div>
  );
}
